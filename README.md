# ikes-edge-website
Public website for Ike's Edge

## Recent updates

- Improved CSS browser compatibility by adding `-webkit-backdrop-filter` and `-webkit-mask-image` alongside the standard properties in `styles.css`.
- Removed an unnecessary mobile `.hero` `min-height: auto;` override to keep the layout behavior consistent across browsers.
